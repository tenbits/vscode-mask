{
    "name": "language-mask",
    "version": "0.0.2",
    "engines": {
        "vscode": ">=1.0.0"
    },
    "publisher": "alexkit",
    "contributes": {
        "languages": [{
            "id": "mask",
            "aliases": ["Mask", "mask"],
            "extensions": [".mask"],
            "configuration": "./mask-language-configuration.json"
        }],
        "grammars": [{
            "language": "mask",
            "scopeName": "source.mask",
            "path": "./syntaxes/mask.tmLanguage"
        }]
    }
}
